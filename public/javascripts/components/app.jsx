var React = require("react");
var Search = require("./search.jsx");

var App = React.createClass({
    render: function() {
        return (
            <Search />
        );
    }
});

module.exports = App;